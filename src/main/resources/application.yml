spring:
  application:
    name:18May

  datasource:
    hikari:
      jdbc-url: jdbc:h2:./appdb
      username: sa
      password:
  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8080

