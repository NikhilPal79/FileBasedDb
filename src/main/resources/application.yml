spring:
  application:
    name:18May

  datasource:
    hikari:
      jdbc-url: jdbc:h2:./appdb
      username: sa
      password:
  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8080

